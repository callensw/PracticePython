def is_prime():
    num = int(input('Insert a number: '))
    a = [x for x in range(2, num) if num % x == 0]

    if num > 1:
        if len(a) == 0:
            print('prime')
        else:
            print('NOT prime')
    else:
        print('NOT prime')

is_prime()
